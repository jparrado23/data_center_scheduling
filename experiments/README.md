# Experiments

Use `experiments/configs/` for experiment configuration files and `experiments/outputs/` for generated results.

The first version is notebook-driven. Scripted experiment runners can be added after the MILP baseline is validated.
